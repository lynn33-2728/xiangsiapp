# 长记忆四入口设计验收

- reference: `C:\Users\Calvin\AppData\Local\Temp\codex-clipboard-967c1342-eece-4e1d-9ae2-6e4bd76ad20e.png`
- implementation: `http://127.0.0.1:5193/`，Codex 应用内浏览器手机宽度实测
- final result: passed

## 对照结果

- 四个入口保持在同一横排，使用一个共用外框，格子间只有单线分隔，未出现卡片空隙或换行。
- 选中项沿用 App 现有绿色强调色，四项文字在手机宽度内可读。
- 点击“本角色记忆库”“本聊天框数据”“数据表与提示词”“记忆 API”时，仅对应内容在整行入口下方展开。
- “本角色记忆库”上传入口、当前聊天数据编辑、真实字段与提示词查看、记忆 API 摘要均可到达。
- 深色主题下未发现裁切、遮挡、横向溢出或不可读文字。

## 功能核验

- 小说、剧本、旧聊天资料按 `roleId` 保存和召回，不再作为所有角色共享资料。
- 旧版 `knowledge` 资料在启动加载时迁移为当前角色资料。
- 当前聊天上传资料仍按 `chatId` 隔离。
- JavaScript 语法检查通过。
