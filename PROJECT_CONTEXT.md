# 相思 App 项目上下文

> 最后更新：2026-09-13
>
> 用于让新的 Codex、网页版 ChatGPT 或其他协作者快速接手。请先阅读，再修改源码。本文件不包含 API 密钥。

## 1. 项目目标

相思 App 是以角色扮演聊天为核心的前端/Android WebView 应用。重点是让角色在长期剧情中保持状态、人物关系和原作本色一致，同时避免照抄原台词或重演原剧情。

## 2. 仓库与关键文件

- 工作区：`C:\Users\Calvin\Documents\元宝复刻`
- 应用目录：`C:\Users\Calvin\Documents\元宝复刻\相思App`
- `相思App/index.html`：页面结构与脚本入口。
- `相思App/styles.css`：全局、手机、浅色/深色主题样式。
- `相思App/factory-default-config.js`：出厂默认数据。
- `相思App/app-memory.js`：长记忆、知识库、整理、召回、总结 API 和记忆请求组合。
- `相思App/app.js`：聊天界面、角色、预设、世界书、语音、启动逻辑及长记忆界面层。
- `相思App/serve.js`、`相思App/开启预览.bat`：本机预览，通常为 `http://127.0.0.1:5193/`。
- `相思App/tests/browser-memory-self-test.mjs`：浏览器长记忆自检。
- `相思App/tests/finalization-risk-check.mjs`：离线资源、脚本拆分和 APK 资源检查。
- `相思App/tests/transient-action-menu-check.mjs`：临时菜单回归检查。

脚本顺序必须保持：`factory-default-config.js` → `app-memory.js` → `app.js`。

当前 `index.html` 缓存编号为 `v=100`。生产脚本变化后要同步修改缓存编号和 `finalization-risk-check.mjs` 的断言，避免手机继续加载旧代码。

## 3. 数据安全红线

除非用户明确要求，禁止：

- 删除或清空 IndexedDB、localStorage、聊天历史、知识库或整理成果。
- 自动覆盖旧知识库/RAG 语料，或为修界面而重置配置。
- 使用 `git reset --hard`、`git checkout --` 等破坏性命令。
- 输出、记录或截图 API Key。

知识库主要保存在浏览器 IndexedDB：数据库 `xiangsi_memory_v1`，store `documents`。聊天设置、选择和运行日志还会使用 localStorage。页面显示“没有资料”不代表数据库被清空，必须先确认 `loadMemoryDocuments()` 是否完成。

用户在 2026-09-13 电脑多次死机。接手时优先做只读检查和小补丁，不要立即重建索引、重整整部剧本或删除资料。

## 4. 长记忆真实结构

### 当前聊天动态记忆

【本聊天框数据】只保留：

1. 当前时间与地点
2. 人物与关系
3. 未完剧情
4. 自定义状态
5. 剧情纪要

自动更新与发送给聊天 AI 是两个独立开关。全关也保留数据；重新开启不归零。启用发送的状态合并成一个【当前聊天状态】块。

### 知识库三种用途

1. 【角色本色卡】：短内容，启用角色每轮常驻。
2. 【情境演绎】：按当前情境召回类似场合中的思考、语言和动作。
3. 【原作事实】：身份、关系、地点、物品和剧情事实按需召回。

注入优先级不得改变：

`当前聊天状态与关系 > 当前启用人设 > 常驻角色本色卡 > 情境演绎 > 本聊天剧情纪要 > 原作事实`

当前聊天已经发生的内容永远优先，知识库不得覆盖当前剧情。

### 人物关系

- 一对人物一张自由文本关系卡。
- 正式变化历史永久保留。
- 只有告白、成婚、相认、背叛、结盟等正式变化才新增历史。
- 原作关系与聊天关系分层；聊天变化不修改知识库原作。

## 5. 普通小说/剧本整理

上传只保存和分块，不自动调用 AI。用户确认目标角色后手动整理：

1. 本机按主名和已确认别名筛选场景。
2. 每批最多约 5000 字，调用总结 API。
3. 保存情境演绎卡、事实和候选别名。
4. 接受候选别名后补扫新别名命中的未处理场景。
5. 场景完成后归纳短角色本色卡。
6. 每步保存；停止或网络失败后从未处理场景继续。

演绎卡固定保存九项：情境标签、关系阶段、触发事件、内在动机、外在反应、语言方式、动作方式、避免行为、原文证据。

【数据与提示词】里的九项复选框只过滤最终发送给聊天 AI 的字段，不改变提取结构，也不删除卡片。

## 6. RAG 语料定位

【RAG语料】是资料来源，不是第四套结果，也不等于 AI 生成的九项演绎卡。

已实现：

- 类型可选【RAG语料】，支持 JSONL 或 JSON 对象数组。
- 正文兼容 `text/content/document/body`；人物兼容 `people/characters/character/roles`。
- 场景、时间、地点、重要度可选。
- 导入前显示字段对应、总数、有效数、缺正文数、人物统计和前三条预览。
- 确认后逐条保存，不调用总结 API。
- `people` 含目标角色/别名：进入【情境参考】候选池。
- 仅正文提及角色：只进入【原作事实】候选池。
- 同一条同一轮不会在情境与事实重复注入。
- 确认别名后即时参与归属，无需重新调用 AI。
- 详情显示【查看语料】【检查字段】【重新导入】，不显示【开始整理】。
- 重新导入创建新资料并保留旧资料，待用户确认后手动删除旧资料。

真实样本：`C:\Users\Calvin\Desktop\元宝复活资料\剧本。语料\剧-RAG语料-相柳.jsonl`

最近验证：318 行，317 条有效，1 条缺正文；六个字段正确识别。

## 7. 当前问题：整理进度增加但零成果

现场症状：普通资料整理显示约 `50/76`，但本色卡和演绎卡为 0。

已确认旧漏洞：模型返回无法识别或零结果时，旧代码仍把文档写入 `processedByTarget.docIds` 并增加 `runCompleted`，造成“进度成功、成果为零”。电脑死机会造成请求硬中断和遗留 `running` 状态，但不能单独解释连续 50 批零卡；两种问题可能叠加。

当前工作区已加入的修复：

1. 兼容 `data/result/output` 包裹、camelCase 和 snake_case。
2. 支持 `performance_cards`、`target_name`、`situation_tags`、`trigger_event`、`inner_motive`、`outward_response`、`speech_pattern`、`action_pattern`、`avoid_behavior`、`original_excerpt` 等常见返回字段。
3. JSON 没有约定结构时立即停止，当前批不计进度。
4. 连续 5 批没有任何演绎卡时停止空转，当前批不计进度，并在本机运行日志保留响应摘要。
5. 对旧的“运行中/停止/失败、至少 3 步、本色卡和演绎卡都为 0”状态：下一次继续时清除假进度并重跑原场景；不删除原文、人物目录、关系或事实。
6. 真正 `ready` 的完成资料不会自动重跑。

限制：旧版本没有保存每批完整响应，已经误计的 50 批无法直接还原，只能在加载新代码后重新处理。

最近 TDD 状态：修复前新增测试按预期失败；修复后长记忆浏览器自检 `84/84`。随后缓存升至 `v=100`。最后合跑时发现 `127.0.0.1:5193` 因死机停止并拒绝连接，所以还需要恢复预览后重新合跑；不能声称真实总结 API 已验证。

用户恢复步骤：

1. 停止旧页面仍显示的整理任务。
2. 恢复预览并强制刷新，让 `v=100` 生效。
3. 确认目标角色“相柳”已勾选。
4. 点【继续整理】，先观察 1～3 批是否出现演绎卡，不要立即跑完整资料。
5. 若仍为空，导出【设置 → 运行日志】；只分析模型名、返回长度、结束原因和响应摘要，不分享密钥。

## 8. 知识库空白页修复

曾出现界面显示没有知识库，但 IndexedDB 实际存在。根因是详情页可能在 `loadMemoryDocuments()` 完成前渲染，加载后旧代码只刷新历史列表。

当前修复：增加 `memoryDocumentsReady`；加载中显示读取提示；加载完成且仍在【长记忆】时自动重新打开当前详情。禁止用清库或重传解决空白页。

## 9. 提示词管理

- 完整编辑入口只在【数据与提示词】。
- 其他页面只显示当前版本和跳转入口。
- 用户版本全局共享；聊天分别保存选择；知识库每份资料可选不同整理提示词。

尚未实施的方案：把硬编码【记忆使用规则】改成可编辑请求提示词，并在知识库详情加入两个请求侧入口。方案位于：

`C:\Users\Calvin\.workbuddy\plans\blazing-thunder-babbage - 副本，想修改的方案.md`

当前源码仍有硬编码 `priorityRule`，不存在 `memoryPriority` target，说明另一位 AI 没有实际落地该方案。不要在修复零成果时顺手实施。

用户提到的 `C:\Users\Calvin\Desktop\被删除文件audit-memory-2026-09-05` 仅含审查 Markdown 和截图，不是 App 运行依赖，删除它不会造成整理失效。

## 10. 不应改变

- 不建立第四套 RAG 结果。
- 不让上传自动调用总结 API。
- 不把多人轮询直接改成群聊。
- 不混写原作关系与聊天关系。
- 不恢复固定七表整套输出。
- 不用最终 `slice(0, 12000)` 从句子/JSON 中间硬截断。
- 不为制造成果而编造关系、性格或别名。
- 不在没有真实响应证据时盲改提示词。
- 本轮不生成正式签名 APK。

## 11. 验证方式

先恢复本机预览，再运行：

```powershell
node --check 相思App/app.js
node --check 相思App/app-memory.js
node --check 相思App/tests/browser-memory-self-test.mjs
node --check 相思App/tests/finalization-risk-check.mjs
node --check 相思App/tests/transient-action-menu-check.mjs
node 相思App/tests/browser-memory-self-test.mjs "C:\Users\Calvin\Desktop\元宝复活资料\剧本。语料\剧-RAG语料-相柳.jsonl"
node 相思App/tests/finalization-risk-check.mjs
node 相思App/tests/transient-action-menu-check.mjs
```

预期静态/模拟结果：长记忆 `84/84`；语料 `317/318 valid, 1 missing text`；离线资源和菜单检查通过。这不等于真实 API 通过。真实 API 仅用短样本跑 1～3 批，确认会生成卡片且失败不推进后再扩大。

## 12. 协作原则与下一步

- 先找根因；新行为先写失败测试，再做最小修复。
- 使用 `rg` 定位，不反复整份读取近百万字的 `app.js`。
- 使用补丁修改，不整文件覆盖；保留用户现有改动。
- 页面能打开不等于 Android 实机通过。

下一步：

1. 恢复 `127.0.0.1:5193` 预览服务。
2. 确认页面实际加载 `v=100`。
3. 重跑第 11 节验证。
4. 短样本真实整理 1～3 批。
5. 如失败，根据运行日志的真实响应摘要补兼容，失败批不得推进。
6. 零成果问题稳定后，再单独实施请求提示词可编辑化。
